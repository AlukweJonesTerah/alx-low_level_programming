#include <stdio.h>
/**
 * main - this main function prints a string of words
 *
 * Return: returns 0 if success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
