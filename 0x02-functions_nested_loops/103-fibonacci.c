#include <stdio.h>
/**
 * main - finds and prints the sum of the even-valued terms
 *
 * Return: 0 is success
 */
int main(void)
{
	int i = 0;
	int j = 0, k = 1, sum = 0;

	while (i < 4000000)
	{
		if (k % 2 == 0)
			sum += i;

		j = k;
		i = a + b;
		k = i;
	}
	printf("%d\n", sum);
	return (0);
}
