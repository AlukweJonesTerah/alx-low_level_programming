#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - Write a function that returns the length of a string.
 * @s: pointer var
 * Return: length
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
